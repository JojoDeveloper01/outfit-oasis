<section
    id="hero"
    class="h-[calc(100dvh-5rem)] bg-[--background] flex items-center justify-center relative overflow-hidden"
>
    <!-- Background Animation -->
    <div class="absolute inset-0">
        <div
            class="absolute top-10 left-10 w-20 h-20 bg-[--color2] rounded-full animate-bounce"
        >
        </div>
        <div
            class="absolute bottom-20 right-20 w-32 h-32 bg-[--color3] rounded-full animate-pulse"
        >
        </div>
        <div
            class="absolute top-1/3 left-1/2 w-40 h-40 bg-[--teal] rounded-full opacity-50 animate-spin-slow"
        >
        </div>
    </div>

    <!-- Content -->
    <div class="grid gap-8 relative text-center z-10">
        <!-- Headline -->
        <h1
            class="text-7xl font-bold text-[--color-black] mb-6 animate-fade-in"
        >
            Revolutionize Your Wardrobe
        </h1>

        <!-- Subheading -->
        <p
            class="text-xl text-center text-[--color4] mb-8 animate-fade-in delay-500"
        >
            Find the perfect outfit for any occasion with ease and elegance.
        </p>

        <!-- Call-to-Action Buttons -->
        <div class="flex justify-center space-x-4">
            <!-- Main CTA Button -->
            <button
                class="px-6 py-3 text-lg font-semibold text-[--color-white] bg-[--teal] rounded-lg hover:bg-[--color3] transition transform hover:scale-105"
            >
                Rent Your Outfit Now
            </button>
        </div>
    </div>
    <!-- Secondary Link -->
    <a
        href="#benefits"
        class="absolute bottom-[4%] text-lg font-medium text-[--color3] underline hover:text-[--gold] transition"
    >
        Explore Our Style Journey
    </a>
</section>
