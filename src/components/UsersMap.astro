---
import PreviewImage from "./Modal/previewImage.astro";

const { users } = Astro.props;
---

<style>
    .col-wrap {
        display: flex;
        gap: 1rem;

        & > * {
            flex: 1;
        }
    }

    @media screen and (max-width: 900px) {
        .col-wrap {
            display: grid;

            & > * {
                width: 10 0%;
            }
        }
    }
</style>

<section>
    <div class="flex gap-4 mt-16 *:flex-1">
        <div class="mb-4">
            <label for="name-filter" class="block text-sm font-medium text-gray-700">
                Search (Name, Email, Phone)
            </label>
            <input
                id="name-filter"
                type="text"
                placeholder="Search by name, email, or phone"
                oninput="filterUsers()"
                class="mt-1 block w-full rounded-md border-gray-300 shadow-sm"
            />
        </div>
        
        <div class="mb-4">
            <label for="type-filter" class="block text-sm font-medium text-gray-700">
                Filter by Type:
            </label>
            <select id="type-filter" onchange="filterUsers()" class="mt-1 block w-full rounded-md border-gray-300 shadow-sm">
                <option value="">All</option>
                <option value="client">Client</option>
                <option value="staff">Staff</option>
            </select>
        </div>
    </div>

    <div class="overflow-x-auto mt-4">
        <table class="min-w-full bg-white border border-gray-200 rounded-lg">
            <thead class="bg-gray-50">
                <tr>
                    <th class="py-2 px-4 text-left text-sm font-semibold text-gray-600 border-b">
                        Profile Picture
                    </th>
                    <th class="py-2 px-4 text-left text-sm font-semibold text-gray-600 border-b">
                        Name
                    </th>
                    <th class="py-2 px-4 text-left text-sm font-semibold text-gray-600 border-b">
                        Email
                    </th>
                    <th class="py-2 px-4 text-left text-sm font-semibold text-gray-600 border-b">
                        User Type
                    </th>
                    <th class="py-2 px-4 text-left text-sm font-semibold text-gray-600 border-b">
                        Phone
                    </th>
                    <th class="py-2 px-4 text-left text-sm font-semibold text-gray-600 border-b">
                        Actions
                    </th>
                </tr>
            </thead>
            <tbody id="user-list" class="divide-y divide-gray-200">
            {users.map((user: any) => (
                
                <tr class="hover:bg-gray-100">
                    <!-- Foto de Perfil -->
                    <PreviewImage src={user.profile_pic || "https://via.placeholder.com/40"} />        
                    <td class="py-3 px-4">
                        <img
                            onclick={`document.getElementById('preview-profile-image-${user.profile_pic || "https://via.placeholder.com/40"}').showModal()`}
                            src={user.profile_pic || "https://via.placeholder.com/40"}
                            alt="Profile"
                            class="h-10 w-10 rounded-full object-cover cursor-pointer"
                        />
                    </td>

                    <!-- Informações do Usuário -->
                    <td class="py-3 px-4 text-sm text-gray-900">{user.name}</td>
                    <td class="py-3 px-4 text-sm text-gray-600">{user.email}</td>
                    <td class="py-3 px-4 text-sm text-gray-600">{user.user_type}</td>
                    <td class="py-3 px-4 text-sm text-gray-600">
                        {user.phone ? user.phone : "N/A"}
                    </td>

                    <!-- Botões de Ações -->
                    <td class="py-3 px-4 flex flex-wrap gap-2">
                        <form action="edit" method="post">
                            <input type="hidden" name="action" value="edit" />
                            <input type="hidden" name="id" value={user.user_id} />
                            <button
                                type="submit"
                                class="bg-blue-500 text-white px-2 py-1 rounded-md hover:bg-blue-600"
                            >
                                Edit
                            </button>
                        </form>
                        <form action="delete" method="post">
                            <input type="hidden" name="action" value="delete" />
                            <input type="hidden" name="id" value={user.user_id} />
                            <button
                                type="submit"
                                class="bg-red-500 text-white px-2 py-1 rounded-md hover:bg-red-600"
                            >
                                Delete
                            </button>
                        </form>
                    </td>
                </tr>
            ))}
    </tbody>

        </table>
    </div>
</section>