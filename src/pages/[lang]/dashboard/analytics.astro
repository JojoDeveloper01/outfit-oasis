---
import BestSeller from "@components/BestSeller.astro";
import { getLangFromUrl } from "@i18n/utils";
import Dashboard from "@layouts/AsideDashboard.astro";
import { getAnalyticsData } from "@lib/dbFunctions";
const lang = getLangFromUrl(Astro.url);

// Busca dados para exibição
const analyticsData = await getAnalyticsData();
const {
    usersWhoPurchased,
    topPurchasedProducts,
    topRenters,
    totalRentalEarnings,
} = analyticsData;

/* console.log("usersWhoPurchased: ", usersWhoPurchased);
console.log("topPurchasedProducts: ", topPurchasedProducts);
console.log("topRenters: ", topRenters);
console.log("totalRentalEarnings: ", totalRentalEarnings); */
---

<Dashboard>
    <h1 class="text-4xl">Analytics</h1>

    <section class="flex flex-wrap gap-[1vw] mt-16">
        <BestSeller lang={lang} topPurchasedProducts={topPurchasedProducts} />

        <!-- Utilizadores com mais aluguéis -->
        <div
            class="min-w-[200px] max-w-[300px] h-fit bg-[--gray] text-white px-[1vw] py-8 rounded-lg shadow-lg"
        >
            <h2 class="text-[1.2vw] font-semibold mb-8">
                Top Utilizadores (Aluguéis)
            </h2>
            <ul class="max-h-[25rem] overflow-y-auto my-4">
                {
                    Array.from(Array.isArray(topRenters) ? topRenters : []).map(
                        (renter: any) => (
                            <li class="flex justify-between">
                                <span>{renter.name}</span>
                                <span class="font-bold">
                                    {renter.count} aluguéis
                                </span>
                            </li>
                        ),
                    )
                }
            </ul>
            <div class="mt-[.9vw] ml-2">
                <a
                    href={`/${lang}/dashboard/users`}
                    class="text-blue-400 hover:underline"
                >
                    Go to users
                </a>
            </div>
        </div>

        <!-- Número de utilizadores que compraram -->
        <div
            class="max-w-[300px] h-fit bg-[--gray] text-white px-[1vw] py-8 rounded-lg shadow-lg"
        >
            <h2 class="text-[1.2vw] font-semibold mb-8">
                Utilizadores que compraram em total
            </h2>
            <p class="text-[2vw] font-bold">{usersWhoPurchased}</p>
        </div>

        <!-- Ganhos totais de aluguéis -->
        <div
            class="min-w-[200px] max-w-[300px] h-fit bg-[--gray] text-white px-[1vw] py-8 rounded-lg shadow-lg"
        >
            <h2 class="text-[1.2vw] font-semibold mb-8">
                Ganho Total (Aluguéis)
            </h2>
            <p class="text-[2vw] font-bold">€ {totalRentalEarnings}</p>
        </div>
    </section>
</Dashboard>
